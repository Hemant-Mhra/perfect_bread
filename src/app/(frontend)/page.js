import { AboutSection, HeroSection, JourneySection } from "@/components";

export default function Home() {
  return (
    <div>
      <HeroSection />
      <AboutSection />
      <JourneySection />
    </div>
  );
}
