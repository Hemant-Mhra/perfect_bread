import {blockContentType} from './blockContentType'
import {postType} from './postType'
import { timelineType } from './timelineType'

export const schema = {
  types: [blockContentType,timelineType, postType],
}
